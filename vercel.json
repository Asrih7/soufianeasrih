{
  "installCommand": "npm install",
  "buildCommand": "npx ng build --configuration production",
  "outputDirectory": "dist/soufiane-portfolio",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}