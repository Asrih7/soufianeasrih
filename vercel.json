{
  "installCommand": "npm install",
  "buildCommand": "npm run build:prod && node move-build.js",
  "outputDirectory": "dist/soufiane-portfolio",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html", "status": 200 }
  ]
}
